

CLASSPATH="./bin:$(grep '\.jar"' .classpath | sed -e 's/^.*"\([^"]*\.jar\)".*/\1:/'|tr -d '\n')"
JAVAOPTS=""

if [ "$1" = "-ea" ]; then
  shift
  JAVAOPTS="-ea"
fi

java $JAVAOPTS -classpath "$CLASSPATH" org.nuthatchery.analysis.java.extractor.ExtractApi "$@"
